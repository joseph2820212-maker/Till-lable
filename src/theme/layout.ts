// Shared layout constants for bottom action bars / content spacing.
//
// These values mirror the Daily Book screens, which are the visual standard
// for the whole app. Plain numbers are intentional here — the goal is to match
// the existing Daily Book bottom-action look exactly, not to re-token it.

export const layout = {
  tabBarHeight: 88,
  bottomActions: {
    paddingHorizontal: 16,
    paddingTop: 12,
    aboveTabBarPaddingBottom: 8,
    deviceEdgePaddingBottom: 12,
    gap: 12,
    contentGap: 100,
  },
};
